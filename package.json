{
  "name": "roadwe-saas",
  "version": "1.0.0",
  "description": "Transport Management SaaS inspired by Roadwe",
  "main": "backend/server.js",
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
